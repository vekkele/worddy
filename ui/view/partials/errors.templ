package partials

templ FieldError(message string) {
	if message != "" {
		<div class="mt-3 text-red-500">{ message }</div>
	}
}

templ FormError(message string) {
	if message != "" {
		<div class="px-4 py-3 text-red-800 bg-red-300 rounded-lg">{ message }</div>
	}
}
